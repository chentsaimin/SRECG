# SRECG 1.1 minimal runtime requirements
# The uploaded full environment freeze is preserved as requirements.full.freeze.txt.
# This file intentionally avoids host-local `file://` package references and unrelated packages.

numpy>=1.24,<3
pandas>=2.0
scipy>=1.10
scikit-learn>=1.2
matplotlib>=3.7
tqdm>=4.66
torch>=2.0
jupyter>=1.0
nbformat>=5.0
